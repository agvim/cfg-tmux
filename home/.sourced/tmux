# reset max bandwith referene for network load monitor
# for the shell version
# alias tmr='$HOME/.tmux/sysstats/tmd.sh reset'
# for the rust version
alias tmr='$HOME/.tmux/rsysstats/target/release/rsysstats -r'
# alias load the proper themed tmux config
alias tmux='tmux -f $HOME/.tmux/tmux-$THEME.conf'
